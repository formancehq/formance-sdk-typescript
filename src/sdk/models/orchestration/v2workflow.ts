/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../../lib/primitives.js";
import { safeParse } from "../../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  V2WorkflowConfig,
  V2WorkflowConfig$inboundSchema,
} from "./v2workflowconfig.js";

export type V2Workflow = {
  v2WorkflowConfig: V2WorkflowConfig;
  createdAt: Date;
  id: string;
  updatedAt: Date;
};

/** @internal */
export const V2Workflow$inboundSchema: z.ZodType<
  V2Workflow,
  z.ZodTypeDef,
  unknown
> = z.object({
  config: V2WorkflowConfig$inboundSchema,
  createdAt: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  id: z.string(),
  updatedAt: z.string().datetime({ offset: true }).transform(v => new Date(v)),
}).transform((v) => {
  return remap$(v, {
    "config": "v2WorkflowConfig",
  });
});

export function v2WorkflowFromJSON(
  jsonString: string,
): SafeParseResult<V2Workflow, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => V2Workflow$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'V2Workflow' from JSON`,
  );
}
