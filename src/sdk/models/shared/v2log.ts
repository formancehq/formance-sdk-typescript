/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  V2LogDataDeleteMetadata,
  V2LogDataDeleteMetadata$inboundSchema,
} from "./v2logdatadeletemetadata.js";
import {
  V2LogDataInsertedSchema,
  V2LogDataInsertedSchema$inboundSchema,
} from "./v2logdatainsertedschema.js";
import {
  V2LogDataNewTransaction,
  V2LogDataNewTransaction$inboundSchema,
} from "./v2logdatanewtransaction.js";
import {
  V2LogDataRevertedTransaction,
  V2LogDataRevertedTransaction$inboundSchema,
} from "./v2logdatarevertedtransaction.js";
import {
  V2LogDataSetMetadata,
  V2LogDataSetMetadata$inboundSchema,
} from "./v2logdatasetmetadata.js";

/**
 * The payload of the log entry. Structure depends on the log type:
 *
 * @remarks
 * - NEW_TRANSACTION: V2LogDataNewTransaction
 * - SET_METADATA: V2LogDataSetMetadata
 * - REVERTED_TRANSACTION: V2LogDataRevertedTransaction
 * - DELETE_METADATA: V2LogDataDeleteMetadata
 * - INSERTED_SCHEMA: V2LogDataInsertedSchema
 */
export type V2LogData =
  | V2LogDataSetMetadata
  | V2LogDataDeleteMetadata
  | V2LogDataNewTransaction
  | V2LogDataRevertedTransaction
  | V2LogDataInsertedSchema;

/**
 * The type of operation this log represents
 */
export enum V2LogType {
  NewTransaction = "NEW_TRANSACTION",
  SetMetadata = "SET_METADATA",
  RevertedTransaction = "REVERTED_TRANSACTION",
  DeleteMetadata = "DELETE_METADATA",
  InsertedSchema = "INSERTED_SCHEMA",
}

/**
 * Represents an immutable log entry in the ledger. Each log captures an atomic operation
 *
 * @remarks
 * with its full payload, enabling audit trails and event sourcing patterns.
 * The data field structure depends on the log type.
 */
export type V2Log = {
  /**
   * The payload of the log entry. Structure depends on the log type:
   *
   * @remarks
   * - NEW_TRANSACTION: V2LogDataNewTransaction
   * - SET_METADATA: V2LogDataSetMetadata
   * - REVERTED_TRANSACTION: V2LogDataRevertedTransaction
   * - DELETE_METADATA: V2LogDataDeleteMetadata
   * - INSERTED_SCHEMA: V2LogDataInsertedSchema
   */
  data:
    | V2LogDataSetMetadata
    | V2LogDataDeleteMetadata
    | V2LogDataNewTransaction
    | V2LogDataRevertedTransaction
    | V2LogDataInsertedSchema;
  /**
   * Timestamp when the operation was recorded
   */
  date: Date;
  /**
   * SHA256 hash of the log entry, chained from the previous log for integrity verification
   */
  hash: string;
  /**
   * Unique sequential identifier for this log entry within the ledger
   */
  id: bigint;
  /**
   * Schema version used for validation when the log was created
   */
  schemaVersion?: string | undefined;
  /**
   * The type of operation this log represents
   */
  type: V2LogType;
};

/** @internal */
export const V2LogData$inboundSchema: z.ZodType<
  V2LogData,
  z.ZodTypeDef,
  unknown
> = z.union([
  V2LogDataSetMetadata$inboundSchema,
  V2LogDataDeleteMetadata$inboundSchema,
  V2LogDataNewTransaction$inboundSchema,
  V2LogDataRevertedTransaction$inboundSchema,
  V2LogDataInsertedSchema$inboundSchema,
]);

export function v2LogDataFromJSON(
  jsonString: string,
): SafeParseResult<V2LogData, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => V2LogData$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'V2LogData' from JSON`,
  );
}

/** @internal */
export const V2LogType$inboundSchema: z.ZodNativeEnum<typeof V2LogType> = z
  .nativeEnum(V2LogType);

/** @internal */
export const V2Log$inboundSchema: z.ZodType<V2Log, z.ZodTypeDef, unknown> = z
  .object({
    data: z.union([
      V2LogDataSetMetadata$inboundSchema,
      V2LogDataDeleteMetadata$inboundSchema,
      V2LogDataNewTransaction$inboundSchema,
      V2LogDataRevertedTransaction$inboundSchema,
      V2LogDataInsertedSchema$inboundSchema,
    ]),
    date: z.string().datetime({ offset: true }).transform(v => new Date(v)),
    hash: z.string(),
    id: z.number().transform(v => BigInt(v)),
    schemaVersion: z.string().optional(),
    type: V2LogType$inboundSchema,
  });

export function v2LogFromJSON(
  jsonString: string,
): SafeParseResult<V2Log, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => V2Log$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'V2Log' from JSON`,
  );
}
