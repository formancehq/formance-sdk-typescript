/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { z } from "zod";

export type ServerInfo = {
    version: string;
};

/** @internal */
export namespace ServerInfo$ {
    export type Inbound = {
        version: string;
    };

    export const inboundSchema: z.ZodType<ServerInfo, z.ZodTypeDef, Inbound> = z
        .object({
            version: z.string(),
        })
        .transform((v) => {
            return {
                version: v.version,
            };
        });

    export type Outbound = {
        version: string;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, ServerInfo> = z
        .object({
            version: z.string(),
        })
        .transform((v) => {
            return {
                version: v.version,
            };
        });
}
