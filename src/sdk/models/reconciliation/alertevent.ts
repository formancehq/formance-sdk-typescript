/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export const NewStatus = {
  Open: "OPEN",
  Acknowledged: "ACKNOWLEDGED",
  Resolved: "RESOLVED",
} as const;
export type NewStatus = ClosedEnum<typeof NewStatus>;

export const PrevStatus = {
  Open: "OPEN",
  Acknowledged: "ACKNOWLEDGED",
  Resolved: "RESOLVED",
} as const;
export type PrevStatus = ClosedEnum<typeof PrevStatus>;

export const Type = {
  Fail: "fail",
  Pass: "pass",
  Ack: "ack",
  Resolve: "resolve",
  Accept: "accept",
  Snooze: "snooze",
  Unsnooze: "unsnooze",
} as const;
export type Type = ClosedEnum<typeof Type>;

/**
 * One row of the alert's append-only event log. Captures every
 *
 * @remarks
 * evaluation that touched the alert plus every manual transition
 * (ack / resolve / accept / snooze / unsnooze). `prevStatus` is null
 * only for the inaugural event of an alert. `isReopen` is a derived
 * predicate — true when a FAIL event lands on a previously-RESOLVED alert.
 * `notify` is the notification decision recorded for the row: false marks
 * a transition kept for audit but deliberately not published (a repeated
 * identical fail, or a fail muted by an active snooze).
 */
export type AlertEvent = {
  alertID: string;
  at: Date;
  evaluationID?: string | null | undefined;
  id: string;
  isReopen: boolean;
  newStatus: NewStatus;
  /**
   * Whether this transition was published to the message bus. false = recorded for audit but suppressed.
   */
  notify: boolean;
  /**
   * Event-type-specific payload (evidence for fail, resolution for resolve/accept, ack object for ack, snooze object for snooze).
   */
  payload?: { [k: string]: any } | undefined;
  prevStatus?: PrevStatus | null | undefined;
  type: Type;
};

/** @internal */
export const NewStatus$inboundSchema: z.ZodNativeEnum<typeof NewStatus> = z
  .nativeEnum(NewStatus);

/** @internal */
export const PrevStatus$inboundSchema: z.ZodNativeEnum<typeof PrevStatus> = z
  .nativeEnum(PrevStatus);

/** @internal */
export const Type$inboundSchema: z.ZodNativeEnum<typeof Type> = z.nativeEnum(
  Type,
);

/** @internal */
export const AlertEvent$inboundSchema: z.ZodType<
  AlertEvent,
  z.ZodTypeDef,
  unknown
> = z.object({
  alertID: z.string(),
  at: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  evaluationID: z.nullable(z.string()).optional(),
  id: z.string(),
  isReopen: z.boolean(),
  newStatus: NewStatus$inboundSchema,
  notify: z.boolean(),
  payload: z.record(z.any()).optional(),
  prevStatus: z.nullable(PrevStatus$inboundSchema).optional(),
  type: Type$inboundSchema,
});

export function alertEventFromJSON(
  jsonString: string,
): SafeParseResult<AlertEvent, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AlertEvent$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AlertEvent' from JSON`,
  );
}
