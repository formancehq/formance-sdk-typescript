/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { ClosedEnum } from "../../types/enums.js";

/**
 * Reconciliation rhythm. Scopes each failing fingerprint into a period so a
 *
 * @remarks
 * March break and an April break are distinct, independently-closable cases.
 * `continuous` (default) is a single unbounded period (live monitoring).
 */
export const Cadence = {
  Continuous: "continuous",
  Daily: "daily",
  Weekly: "weekly",
  Monthly: "monthly",
} as const;
/**
 * Reconciliation rhythm. Scopes each failing fingerprint into a period so a
 *
 * @remarks
 * March break and an April break are distinct, independently-closable cases.
 * `continuous` (default) is a single unbounded period (live monitoring).
 */
export type Cadence = ClosedEnum<typeof Cadence>;

/** @internal */
export const Cadence$inboundSchema: z.ZodNativeEnum<typeof Cadence> = z
  .nativeEnum(Cadence);
/** @internal */
export const Cadence$outboundSchema: z.ZodNativeEnum<typeof Cadence> =
  Cadence$inboundSchema;
