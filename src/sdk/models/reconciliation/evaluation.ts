/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import {
  collectExtraKeys as collectExtraKeys$,
  safeParse,
} from "../../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type Evidence = {
  /**
   * Full breakdown. Present on FAIL entries.
   */
  evidence?: { [k: string]: any } | undefined;
  fingerprint?: string | undefined;
  passed?: boolean | undefined;
  /**
   * Compact green-proof (observed balance integers as strings). Present on PASS entries.
   */
  proof?: { [k: string]: string } | undefined;
  additionalProperties?: { [k: string]: any } | undefined;
};

/**
 * For PASS/FAIL: the per-fingerprint roster. Each PASS entry carries a compact `proof`: the observed balances that make the check hold, with self-describing keys — both sides where two are compared, e.g. {"ledger":"523500","pool":"-523500"} (drift), {"left":…,"right":…} (source_parity), {"positive":…,"negative":…} (ledger_invariant), or {"balance":…} (account_threshold). These reuse the FAIL balance keys, so under tolerance the residual (left−right) is visible without recomputation. Each FAIL entry carries the full `evidence` breakdown (balances, drift, tolerance, compiled CEL). Bounded by asset count (per-asset granularity; per-account is parked). An empty array means no outcomes were produced (no assets matched). For ERROR: an object describing the engine failure.
 */
export type EvidenceUnion = Array<Evidence> | { [k: string]: any };

export const Result = {
  Pass: "PASS",
  Fail: "FAIL",
  Error: "ERROR",
} as const;
export type Result = ClosedEnum<typeof Result>;

export type Evaluation = {
  costUnits?: number | undefined;
  createdAt: Date;
  endedAt: Date;
  error?: string | undefined;
  /**
   * For PASS/FAIL: the per-fingerprint roster. Each PASS entry carries a compact `proof`: the observed balances that make the check hold, with self-describing keys — both sides where two are compared, e.g. {"ledger":"523500","pool":"-523500"} (drift), {"left":…,"right":…} (source_parity), {"positive":…,"negative":…} (ledger_invariant), or {"balance":…} (account_threshold). These reuse the FAIL balance keys, so under tolerance the residual (left−right) is visible without recomputation. Each FAIL entry carries the full `evidence` breakdown (balances, drift, tolerance, compiled CEL). Bounded by asset count (per-asset granularity; per-account is parked). An empty array means no outcomes were produced (no assets matched). For ERROR: an object describing the engine failure.
   */
  evidence?: Array<Evidence> | { [k: string]: any } | undefined;
  id: string;
  /**
   * Effective historical PITs used by sources. For a Payments `latest` read, the value is the successful observation time because the upstream response has no snapshot timestamp; the evaluation evidence is frozen, but exact historical replay of that latest snapshot is not guaranteed.
   */
  pitPerSource?: { [k: string]: Date } | undefined;
  result: Result;
  ruleID: string;
  startedAt: Date;
};

/** @internal */
export const Evidence$inboundSchema: z.ZodType<
  Evidence,
  z.ZodTypeDef,
  unknown
> = collectExtraKeys$(
  z.object({
    evidence: z.record(z.any()).optional(),
    fingerprint: z.string().optional(),
    passed: z.boolean().optional(),
    proof: z.record(z.string()).optional(),
  }).catchall(z.any()),
  "additionalProperties",
  true,
);

export function evidenceFromJSON(
  jsonString: string,
): SafeParseResult<Evidence, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Evidence$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Evidence' from JSON`,
  );
}

/** @internal */
export const EvidenceUnion$inboundSchema: z.ZodType<
  EvidenceUnion,
  z.ZodTypeDef,
  unknown
> = z.union([z.array(z.lazy(() => Evidence$inboundSchema)), z.record(z.any())]);

export function evidenceUnionFromJSON(
  jsonString: string,
): SafeParseResult<EvidenceUnion, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => EvidenceUnion$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'EvidenceUnion' from JSON`,
  );
}

/** @internal */
export const Result$inboundSchema: z.ZodNativeEnum<typeof Result> = z
  .nativeEnum(Result);

/** @internal */
export const Evaluation$inboundSchema: z.ZodType<
  Evaluation,
  z.ZodTypeDef,
  unknown
> = z.object({
  costUnits: z.number().int().optional(),
  createdAt: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  endedAt: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  error: z.string().optional(),
  evidence: z.union([
    z.array(z.lazy(() => Evidence$inboundSchema)),
    z.record(z.any()),
  ]).optional(),
  id: z.string(),
  pitPerSource: z.record(
    z.string().datetime({ offset: true }).transform(v => new Date(v)),
  ).optional(),
  result: Result$inboundSchema,
  ruleID: z.string(),
  startedAt: z.string().datetime({ offset: true }).transform(v => new Date(v)),
});

export function evaluationFromJSON(
  jsonString: string,
): SafeParseResult<Evaluation, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Evaluation$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Evaluation' from JSON`,
  );
}
