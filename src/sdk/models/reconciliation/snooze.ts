/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * A time-boxed, operator-initiated mute of an alert's notifications. While
 *
 * @remarks
 * `until` is in the future the alert keeps failing and keeps counting
 * against period-green — only its notifications are suppressed.
 */
export type Snooze = {
  at: Date;
  by: string;
  note?: string | undefined;
  until: Date;
};

/** @internal */
export const Snooze$inboundSchema: z.ZodType<Snooze, z.ZodTypeDef, unknown> = z
  .object({
    at: z.string().datetime({ offset: true }).transform(v => new Date(v)),
    by: z.string(),
    note: z.string().optional(),
    until: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  });

export function snoozeFromJSON(
  jsonString: string,
): SafeParseResult<Snooze, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Snooze$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Snooze' from JSON`,
  );
}
