/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../../lib/primitives.js";
import { safeParse } from "../../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  V2LogTransaction,
  V2LogTransaction$inboundSchema,
} from "./v2logtransaction.js";

/**
 * Payload for NEW_TRANSACTION log entries. Contains the created transaction and any account metadata set during creation.
 */
export type V2LogDataNewTransaction = {
  /**
   * Transaction structure as it appears in log payloads
   */
  v2LogTransaction: V2LogTransaction;
  /**
   * Metadata applied to accounts involved in the transaction
   */
  accountMetadata: { [k: string]: { [k: string]: string } };
};

/** @internal */
export const V2LogDataNewTransaction$inboundSchema: z.ZodType<
  V2LogDataNewTransaction,
  z.ZodTypeDef,
  unknown
> = z.object({
  transaction: V2LogTransaction$inboundSchema,
  accountMetadata: z.record(z.record(z.string())),
}).transform((v) => {
  return remap$(v, {
    "transaction": "v2LogTransaction",
  });
});

export function v2LogDataNewTransactionFromJSON(
  jsonString: string,
): SafeParseResult<V2LogDataNewTransaction, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => V2LogDataNewTransaction$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'V2LogDataNewTransaction' from JSON`,
  );
}
