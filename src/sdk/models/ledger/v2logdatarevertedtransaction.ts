/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../../lib/primitives.js";
import { safeParse } from "../../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  V2LogTransaction,
  V2LogTransaction$inboundSchema,
} from "./v2logtransaction.js";

/**
 * Payload for REVERTED_TRANSACTION log entries. Contains both the original reverted transaction and the new reverting transaction.
 */
export type V2LogDataRevertedTransaction = {
  /**
   * Transaction structure as it appears in log payloads
   */
  v2LogTransaction: V2LogTransaction;
  /**
   * Transaction structure as it appears in log payloads
   */
  v2LogTransaction1: V2LogTransaction;
};

/** @internal */
export const V2LogDataRevertedTransaction$inboundSchema: z.ZodType<
  V2LogDataRevertedTransaction,
  z.ZodTypeDef,
  unknown
> = z.object({
  revertedTransaction: V2LogTransaction$inboundSchema,
  transaction: V2LogTransaction$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "revertedTransaction": "v2LogTransaction",
    "transaction": "v2LogTransaction1",
  });
});

export function v2LogDataRevertedTransactionFromJSON(
  jsonString: string,
): SafeParseResult<V2LogDataRevertedTransaction, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => V2LogDataRevertedTransaction$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'V2LogDataRevertedTransaction' from JSON`,
  );
}
