/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../../lib/primitives.js";
import { safeParse } from "../../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { Subject, Subject$inboundSchema } from "./subject.js";

export type Hold2 = {
  subject?: Subject | undefined;
  asset: string;
  description: string;
  /**
   * The unique ID of the hold.
   */
  id: string;
  /**
   * Metadata associated with the hold.
   */
  metadata: { [k: string]: string };
  /**
   * Original amount on hold
   */
  originalAmount: bigint;
  /**
   * Remaining amount on hold
   */
  remaining: bigint;
  /**
   * The ID of the wallet the hold is associated with.
   */
  walletID: string;
};

/** @internal */
export const Hold2$inboundSchema: z.ZodType<Hold2, z.ZodTypeDef, unknown> = z
  .object({
    destination: Subject$inboundSchema.optional(),
    asset: z.string(),
    description: z.string(),
    id: z.string(),
    metadata: z.record(z.string()),
    originalAmount: z.number().transform(v => BigInt(v)),
    remaining: z.number().transform(v => BigInt(v)),
    walletID: z.string(),
  }).transform((v) => {
    return remap$(v, {
      "destination": "subject",
    });
  });

export function hold2FromJSON(
  jsonString: string,
): SafeParseResult<Hold2, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Hold2$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Hold2' from JSON`,
  );
}
