/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../../lib/primitives.js";
import { safeParse } from "../../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { TaskStatus, TaskStatus$inboundSchema } from "./taskstatus.js";

export type Descriptor = {
  account: string;
  main?: boolean | undefined;
  name: string;
};

export type State = {};

export type TaskBase = {
  taskStatus: TaskStatus;
  connectorID: string;
  createdAt: Date;
  descriptor: Descriptor;
  error?: string | null | undefined;
  id: string;
  state?: State | null | undefined;
  updatedAt: Date;
};

/** @internal */
export const Descriptor$inboundSchema: z.ZodType<
  Descriptor,
  z.ZodTypeDef,
  unknown
> = z.object({
  account: z.string(),
  main: z.boolean().optional(),
  name: z.string(),
});

export function descriptorFromJSON(
  jsonString: string,
): SafeParseResult<Descriptor, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Descriptor$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Descriptor' from JSON`,
  );
}

/** @internal */
export const State$inboundSchema: z.ZodType<State, z.ZodTypeDef, unknown> = z
  .object({});

export function stateFromJSON(
  jsonString: string,
): SafeParseResult<State, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => State$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'State' from JSON`,
  );
}

/** @internal */
export const TaskBase$inboundSchema: z.ZodType<
  TaskBase,
  z.ZodTypeDef,
  unknown
> = z.object({
  status: TaskStatus$inboundSchema,
  connectorID: z.string(),
  createdAt: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  descriptor: z.lazy(() => Descriptor$inboundSchema),
  error: z.nullable(z.string()).optional(),
  id: z.string(),
  state: z.nullable(z.lazy(() => State$inboundSchema)).optional(),
  updatedAt: z.string().datetime({ offset: true }).transform(v => new Date(v)),
}).transform((v) => {
  return remap$(v, {
    "status": "taskStatus",
  });
});

export function taskBaseFromJSON(
  jsonString: string,
): SafeParseResult<TaskBase, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TaskBase$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TaskBase' from JSON`,
  );
}
