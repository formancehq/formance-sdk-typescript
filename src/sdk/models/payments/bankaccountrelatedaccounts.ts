/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type BankAccountRelatedAccounts = {
  /**
   * Identifier of the provider-side account
   */
  accountID: string;
  /**
   * Identifier of the connector holding the provider-side account
   */
  connectorID: string;
  /**
   * When the bank account was forwarded to this provider
   */
  createdAt: Date;
  /**
   * Unique identifier of the link between the bank account and the provider
   */
  id: string;
  /**
   * Name of the payment provider behind the connector
   */
  provider: string;
};

/** @internal */
export const BankAccountRelatedAccounts$inboundSchema: z.ZodType<
  BankAccountRelatedAccounts,
  z.ZodTypeDef,
  unknown
> = z.object({
  accountID: z.string(),
  connectorID: z.string(),
  createdAt: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  id: z.string(),
  provider: z.string(),
});

export function bankAccountRelatedAccountsFromJSON(
  jsonString: string,
): SafeParseResult<BankAccountRelatedAccounts, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => BankAccountRelatedAccounts$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'BankAccountRelatedAccounts' from JSON`,
  );
}
