/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  V3PaymentStatusEnum,
  V3PaymentStatusEnum$inboundSchema,
} from "./v3paymentstatusenum.js";

export type V3PaymentAdjustment = {
  /**
   * Amount carried by this adjustment
   */
  amount?: bigint | undefined;
  /**
   * Asset the adjustment is denominated in
   */
  asset?: string | undefined;
  /**
   * When the adjustment occurred at the provider
   */
  createdAt: Date;
  /**
   * Unique identifier of the adjustment
   */
  id: string;
  /**
   * Arbitrary key/value pairs attached to the resource
   */
  metadata?: { [k: string]: string } | null | undefined;
  /**
   * The provider's original payload for this adjustment
   */
  raw: { [k: string]: any };
  /**
   * Identifier the adjustment carries at the provider
   */
  reference: string;
  /**
   * Where a payment stands in its lifecycle
   */
  status: V3PaymentStatusEnum;
};

/** @internal */
export const V3PaymentAdjustment$inboundSchema: z.ZodType<
  V3PaymentAdjustment,
  z.ZodTypeDef,
  unknown
> = z.object({
  amount: z.number().transform(v => BigInt(v)).optional(),
  asset: z.string().optional(),
  createdAt: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  id: z.string(),
  metadata: z.nullable(z.record(z.string())).optional(),
  raw: z.record(z.any()),
  reference: z.string(),
  status: V3PaymentStatusEnum$inboundSchema,
});

export function v3PaymentAdjustmentFromJSON(
  jsonString: string,
): SafeParseResult<V3PaymentAdjustment, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => V3PaymentAdjustment$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'V3PaymentAdjustment' from JSON`,
  );
}
